import { BandImagePipe } from './band-image.pipe';

describe('BandImagePipe', () => {
  it('create an instance', () => {
    const pipe = new BandImagePipe();
    expect(pipe).toBeTruthy();
  });
});
